# remove unwanted keybinds defined by grml-zsh-config in main .zshrc
bindkey -r '^Ed'

# add local prompt theme directory and set prompt
fpath=($ZDOTDIR/prompts $fpath)
autoload -Uz promptinit
promptinit
prompt kevin
